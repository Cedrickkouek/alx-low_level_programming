#include <stdio.h>
/**
 * main - prints the number of arguments parsed in.
 * @argv: array of pointers to string of arguments
 * @argc: number of arguments
 * Return: Always 0.
 **/
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
